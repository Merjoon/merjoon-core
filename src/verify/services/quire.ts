import { getQuireService } from '../../quire/quire-service';

export const config = {
  dependencies: {
    projects: [],
    users: [],
    tasks: [],
  },
};

export const service = getQuireService();
