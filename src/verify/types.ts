export enum IntegrationId {
  ClickUp = 'clickup',
  Hive = 'hive',
  Jira = 'jira',
  Teamwork = 'teamwork',
  GitLab = 'gitlab',
  Shortcut = 'shortcut',
}

export type EntityName = 'users' | 'projects' | 'tasks';
