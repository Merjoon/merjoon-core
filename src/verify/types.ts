export enum IntegrationId {
  ClickUp = 'clickup',
  GitLab = 'gitlab',
  Hive = 'hive',
  Jira = 'jira',
  Shortcut = 'shortcut',
  Teamwork = 'teamwork',
}

export type EntityName = 'users' | 'projects' | 'tasks';
